language: node_js
node_js:
  - '16'

os:
   #- linux
   #- windows
    - osx

install:
  - npm i -g yarn
  - yarn install
  - yarn run build
  - yarn run dist

deploy:
  draft: true
  edge: true
  file_glob: true
  overwrite: true
  provider: releases
  on:
    tags: true
    all_branches: true
